version: '3.8'
services:
  stellar:
    image: stellar/quickstart:testing
    command: --local --enable-soroban-rpc
    ports:
      - "8000:8000"
    volumes:
      - stellar-data:/opt/stellar
volumes:
  stellar-data:
